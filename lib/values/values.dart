import 'package:flutter/material.dart';

///Values and constants used through the app
///todo: manage this better somehow
class Values {
  static const String dateFormat = 'yyyy/MM/dd';

  //Used in the card splash color
  static final Color cardColor = Colors.teal.withAlpha(30);
}
